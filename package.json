{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:all": "concurrently -n BACK,FRONT -c bgBlue,bgGreen \"python -m uvicorn backend.api.main:app --reload\" \"vite --port 8080\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}